export type Comparable = {
    id: string;
    title: string,
    description: string,
    LRNumber: string,
    type: string,
    userId: string,
    username: string,
    image: string,
    images: [string],   
    address: string,
    amenities: [string],
    views: number,
    verified: Boolean,
    published: Boolean,
    area: number,
    date: string,
    priceType: string,
    price: number
    latitude: number,
    longitude: number,
  };

